target.dir=${basedir}/target
antlibs.dir=${common-build.dir}/antlibs

src.dir=src
test.dir=test
web.dir=WebContent
src.bin.dir=${target.dir}/classes
test.bin.dir=${target.dir}/test/classes

ivy.install.version=2.4.0
ivy.jar.dir=${antlibs.dir}/ivy
ivy.jar.file=${ivy.jar.dir}/ivy.jar

# this is the default location of ivy
ivy.download.url=https://repo1.maven.org/maven2/org/apache/ivy/ivy/${ivy.install.version}/ivy-${ivy.install.version}.jar

